{
  "name": "asset-selector",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "watch": "parcel src/test.html",
    "build": "rm -rf ./dist && parcel build src/test.html --public-url ./"
  },
  "dependencies": {},
  "devDependencies": {
    "parcel": "^2.0.0-beta.2",
    "typescript": "^4.2.3"
  }
}
